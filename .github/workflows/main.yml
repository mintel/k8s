# Generated by `make configure`, please do not edit manually.
"jobs":
  "aws-load-balancer-controller":
    "name": "Generate aws-load-balancer-controller Jsonnet library and docs"
    "runs-on": "ubuntu-latest"
    "steps":
    - "uses": "actions/checkout@v2"
    - "env":
        "DIFF": "true"
        "GEN_COMMIT": "${{ github.ref == 'refs/heads/master' && github.repository == 'mintel/k8s' }}"
        "GIT_COMMITTER_EMAIL": "73711944+minchine@users.noreply.github.com"
        "GIT_COMMITTER_NAME": "minchine"
        "SSH_KEY": "${{ secrets.DEPLOY_KEY }}"
      "run": "make build libs/aws-load-balancer-controller"
  "debugging":
    "name": "Debugging Github Action values"
    "runs-on": "ubuntu-latest"
    "steps":
    - "run": "echo onMaster? ${{ github.ref == 'refs/heads/master' && github.repository == 'mintel/k8s' }}"
    - "run": "echo ${{ github.repository }}"
    - "run": "echo ${{ github.ref }}"
    - "run": "echo ${{ github.event_name }}"
  "kube-prometheus":
    "name": "Generate kube-prometheus Jsonnet library and docs"
    "runs-on": "ubuntu-latest"
    "steps":
    - "uses": "actions/checkout@v2"
    - "env":
        "DIFF": "true"
        "GEN_COMMIT": "${{ github.ref == 'refs/heads/master' && github.repository == 'mintel/k8s' }}"
        "GIT_COMMITTER_EMAIL": "73711944+minchine@users.noreply.github.com"
        "GIT_COMMITTER_NAME": "minchine"
        "SSH_KEY": "${{ secrets.DEPLOY_KEY }}"
      "run": "make build libs/kube-prometheus"
  "kubernetes-external-secrets":
    "name": "Generate kubernetes-external-secrets Jsonnet library and docs"
    "runs-on": "ubuntu-latest"
    "steps":
    - "uses": "actions/checkout@v2"
    - "env":
        "DIFF": "true"
        "GEN_COMMIT": "${{ github.ref == 'refs/heads/master' && github.repository == 'mintel/k8s' }}"
        "GIT_COMMITTER_EMAIL": "73711944+minchine@users.noreply.github.com"
        "GIT_COMMITTER_NAME": "minchine"
        "SSH_KEY": "${{ secrets.DEPLOY_KEY }}"
      "run": "make build libs/kubernetes-external-secrets"
  "vertical-pod-autoscaler":
    "name": "Generate vertical-pod-autoscaler Jsonnet library and docs"
    "runs-on": "ubuntu-latest"
    "steps":
    - "uses": "actions/checkout@v2"
    - "env":
        "DIFF": "true"
        "GEN_COMMIT": "${{ github.ref == 'refs/heads/master' && github.repository == 'mintel/k8s' }}"
        "GIT_COMMITTER_EMAIL": "73711944+minchine@users.noreply.github.com"
        "GIT_COMMITTER_NAME": "minchine"
        "SSH_KEY": "${{ secrets.DEPLOY_KEY }}"
      "run": "make build libs/vertical-pod-autoscaler"
"on":
  "pull_request":
    "branches":
    - "master"
  "push":
    "branches":
    - "master"